# Vocab Coach Agent Guide

## 项目定位

Vocab Coach 是一个本地优先、简洁轻量的背单词项目。它只专注于词汇录入、学习、复习和必要的学习统计，不以成为综合语言学习平台为目标。

项目使用 FSRS 6 计算复习计划。涉及卡片状态、复习评分、到期时间或学习队列的改动时，应继续使用 FSRS 的状态和调度结果，不要另行实现一套互相冲突的复习算法。

## 产品原则

- 保持实现和界面简单，只增加完成核心背词流程所必需的功能。
- 优先保证“录入单词 -> 学习或复习 -> 提交评分 -> 更新计划”这条主路径清晰可靠。
- 避免加入社交、游戏化、复杂账号体系或与背词无直接关系的功能。
- 保持本地优先。SQLite 是默认数据存储，无网络和无 LLM 配置时，核心背词与复习功能仍应可用。
- LLM 只用于辅助补全释义、音标和例句，不应成为保存、查询或复习已有词汇的必要条件。
- 优先选择清楚、直接、容易维护的实现，避免为尚未出现的需求提前设计复杂抽象。

## 面向 Agent 的设计

这个项目除了供用户通过网页使用，也计划作为其他 agent 的词汇学习后端，例如 Hermes、OpenAI agent 或其他支持工具调用的系统。

- 业务逻辑应放在 `vocab_coach/services/` 中，避免与网页界面或特定 agent 框架耦合。
- HTTP API 应保持稳定、明确，并使用结构化的请求和响应，方便 agent 调用。
- 不要把只适用于某个 agent 的提示词、协议或状态写入核心业务服务。
- 新增 agent 能力时，优先复用现有 service 和 API，不要复制背词、复习或 FSRS 逻辑。
- 未来可以增加 MCP server，将查词、添加词汇、获取复习卡片、提交评分和读取统计等能力暴露给 Hermes、OpenAI 等客户端。
- MCP 应作为核心服务之外的适配层；即使没有启动 MCP，网页和 HTTP API 也应正常工作。

## 代码约定

- 使用 Python 3.12–3.14、FastAPI、SQLAlchemy、SQLite 和 `uv`。
- API 路由保持轻量；验证完成后调用 `vocab_coach/services/` 中的业务逻辑。
- 修改数据库模型时，必须同时添加对应的 Alembic migration。
- 不要提交 `.env`、API key、运行时数据库或 `__pycache__` 等本地生成文件。
- 新增或修改核心行为时，应补充相应测试。
- 完成改动后运行：

  ```bash
  uv run pytest
  ```

## 范围判断

实现新需求前，先判断它是否直接服务于词汇录入、学习、复习、查询、导入或必要统计。如果不是，默认不加入核心项目；如确有集成价值，应优先放在独立的 API、MCP 或其他适配层中。
